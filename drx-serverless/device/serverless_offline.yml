service: '${self:custom.project}-${self:custom.function}-lambda'

custom:
  # project specific variables
  vars: ${file(./variables.json)}
  infoVars: ${file(./aws_infra_parameters.json)}
  resourceVars: ${file(./aws_resource_parameters.json)}
  commonVars: ${file(./lambda_common_env_parameters.json)}
  region: ${env:AWS_REGION}
  environment: ${env:ENVIRONMENT}
  stage: ${env:ENVIRONMENT}
  buildNumber: ${env:CI_PIPELINE_IID}
  project: ${env:PROJECT_CODE}
  APIGWAFV2: ${self:custom.resourceVars.apiWafv2}
  function: device
  kmsArn: ${env:PROJECT_CODE}-${env:ENVIRONMENT}-kms-arn
  customDomainName: ${env:PROJECT_CODE}-${env:ENVIRONMENT}-api-custom-domain
  customDomainPath: !Sub ${env:PROVIDER}-${self:custom.vars.releaseVersion}
  apiauthorizerArn: ${env:PROJECT_CODE}-${env:ENVIRONMENT}-apiauthorizer
  deploy_bucket: ${env:DEPLOY_S3_BUCKET}
  cloudWatchRole: !Sub 'arn:aws:iam::${AWS::AccountId}:role/cochlear-general-apigateway-log-role'
  secretName: ${env:PROJECT_CODE}/${env:ENVIRONMENT}/serverless/secrets
  redisHostname: ${env:PROJECT_CODE}-${env:ENVIRONMENT}-redis-url
  redisPort: ${env:PROJECT_CODE}-${env:ENVIRONMENT}-redis-port
  redisSG: ${env:PROJECT_CODE}-${env:ENVIRONMENT}-redis-security-group

  proxyNamePath: '/device-V1'
  proxyPrefixPath: '/v1/patients/me/devices'

  # serverless plugins specific variables
  offline:
    customAuthenticationProvider: '../local-authorizer-${self:custom.environment}'
  serverless-offline:
    httpPort: 3003
    lambdaPort: 3004
    prefix: 'drx/v1/patients/me/devices'
    noPrependStageInUrl: true

  logRetentionInDays: 30
  apiStageTags:
    Owner: ${self:custom.vars.Owner}
    Description: ${self:custom.vars.Description}
    Name: ${self:custom.project}-${self:custom.function}-lambda
    Function: Serverless
    Environment: ${self:custom.environment}
    Product: ${self:custom.vars.Product}
    Resource: DRX
  apigwBinary:
    types: #list of mime-types
      - '*/*'
  prune:
    automatic: true
    number: 3
  associateWaf:
    name: ${self:custom.APIGWAFV2}
    version: V2
  # warmup:
  #   memorySize: 512
  #   role: warmupRole
  #   events:
  #     - schedule: 'cron(0/15 8-17 ? * MON-FRI *)' # Run WarmUp every 5 minutes Mon-Fri between 8:00am and 5:55pm (UTC)
  #   timeout: 20
  #   prewarm: true # Run WarmUp immediately after a deployment

provider:
  name: aws
  runtime: nodejs20.x
  region: ${self:custom.region}
  vpc:
    securityGroupIds:
      - ${self:custom.redisSG}
    subnetIds: ${self:custom.infoVars.subnets}
  # versionFunctions: false
  endpointType: regional
  stage: ${self:custom.stage}
  deploymentBucket:
    name: ${self:custom.deploy_bucket} # Deployment bucket name. Default is generated by the framework
    serverSideEncryption: AES256 # when using server-side encryption
  memorySize: 512
  timeout: 29
  logs:
    restApi:
      roleManagedExternally: true
      role: ${self:custom.cloudWatchRole}
  stackTags:
    Owner: ${self:custom.vars.Owner}
    project: ${self:custom.project}
    Resource: DRX
  tags:
    Description: ${self:custom.vars.Description}
    Name: '${self:custom.project}-${self:custom.function}'
    Function: 'Serverless'
    Environment: ${self:custom.vars.Environment}
    Product: ${self:custom.vars.Product}
    buildNumber: ${self:custom.buildNumber}
  tracing:
    apiGateway: true
    lambda: true
  apiGateway:
    disableDefaultEndpoint: true
  environment:
    AWS_APPCONFIG_EXTENSION_POLL_INTERVAL_SECONDS: 30
    AWS_APPCONFIG_EXTENSION_POLL_TIMEOUT_MILLIS: 3000
    AWS_APPCONFIG_EXTENSION_HTTP_PORT: 2772
    IS_LOCAL: true

plugins:
  # - serverless-plugin-bind-deployment-id
  - serverless-plugin-optimize
  # - serverless-plugin-warmup
  - api-gateway-stage-tag-plugin
  - serverless-plugin-log-retention
  # - serverless-pseudo-parameters
  - ./serverless-plugin/metric
  - serverless-prune-plugin
  - serverless-associate-waf
  - serverless-offline

package: # Optional deployment packaging configuration
  individually: true
  include: # Specify the directories and files which should be included in the deployment package
    - src/**

functions:
  CorsDevicesFunction:
    handler: src/cors-handler.handleRequest
    kmsKeyArn: ${self:custom.kmsArn}
    environment:
      corsAllowedHeaders: ${self:custom.commonVars.corsAllowedHeaders}
      corsAllowedMethods: ${self:custom.commonVars.corsGETAllowedMethods}
      corsOrigin: ${self:custom.commonVars.corsOrigin}
      corsMaxAge: ${self:custom.commonVars.corsMaxAge}
    role: lambdaRole
    events:
      - http:
          path: /
          method: OPTIONS
      - http:
          path: /single
          method: OPTIONS

  CorsServiceRequestFunction:
    handler: src/cors-handler.handleRequest
    kmsKeyArn: ${self:custom.kmsArn}
    environment:
      corsAllowedHeaders: ${self:custom.commonVars.corsAllowedHeaders}
      corsAllowedMethods: ${self:custom.commonVars.corsPOSTAllowedMethods}
      corsOrigin: ${self:custom.commonVars.corsOrigin}
      corsMaxAge: ${self:custom.commonVars.corsMaxAge}
    role: lambdaRole
    events:
      - http:
          path: /service-requests
          method: OPTIONS

  GetDevicesList:
    handler: src/handler.getDevicesList
    layers:
      - arn:aws:lambda:ap-southeast-2:080788657173:layer:AWS-AppConfig-Extension:91
    kmsKeyArn: ${self:custom.kmsArn}
    environment:
      region: ${self:custom.region}
      boomiOauthEndpoint: ${self:custom.commonVars.boomiOauthEndpoint}
      boomiServiceEndpoint: ${self:custom.commonVars.boomiServiceEndpoint}
      Environment: ${self:custom.environment}
      PMEndpoint: ${self:custom.vars.pmEndpoint}
      SFHostname: ${self:custom.commonVars.sfHostname}
      SFClientID: ${self:custom.commonVars.sfClientID}
      SFUsername: ${self:custom.commonVars.sfUsername}
      secretName: ${self:custom.secretName}
      RedisPort: ${self:custom.redisPort}
      RedisHostname: ${self:custom.redisHostname}
      userSessionSeconds: ${self:custom.vars.userSessionSeconds}
      tokenSessionSeconds: ${self:custom.vars.tokenSessionSeconds}
      redisTokenKey: ${self:custom.commonVars.redisTokenKey}
      userSessionPrefix: ${self:custom.commonVars.userSessionPrefix}
      releaseVersion: ${self:custom.vars.releaseVersion}
      logLevel: ${self:custom.vars.logLevel}
      errorUsers: ${self:custom.vars.errorUsers}
      deviceSupportCountries: ${self:custom.vars.deviceSupportCountries}
      appConfigApplicationId: ${self:custom.vars.appConfigApplicationId}
      appConfigEnvironmentId: ${self:custom.vars.appConfigEnvironmentId}
      appConfigUrl: ${self:custom.vars.appConfigUrl}
    role: lambdaRole
    events:
      - http:
          path: /
          method: get
          cors:
            origins: '*'
            headers:
              - Content-Type
              - x-api-token
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - authorizationtoken
            allowCredentials: false
          authorizer:
            name: authorizer
            arn: ${self:custom.apiauthorizerArn}
            type: request
            resultTtlInSeconds: 0
            identitySource: method.request.header.Authorization, method.request.querystring.id
    # warmup:
    #   prewarm: true
    #   enabled:
    #     - uat
    #     - prd
    #     - dev

  GetDevice:
    handler: src/handler.getDevice
    layers:
      - arn:aws:lambda:ap-southeast-2:080788657173:layer:AWS-AppConfig-Extension:91
    environment:
      region: ${self:custom.region}
      boomiOauthEndpoint: ${self:custom.commonVars.boomiOauthEndpoint}
      boomiServiceEndpoint: ${self:custom.commonVars.boomiServiceEndpoint}
      Environment: ${self:custom.environment}
      PMEndpoint: ${self:custom.vars.pmEndpoint}
      SFHostname: ${self:custom.commonVars.sfHostname}
      SFClientID: ${self:custom.commonVars.sfClientID}
      SFUsername: ${self:custom.commonVars.sfUsername}
      secretName: ${self:custom.secretName}
      RedisPort: ${self:custom.redisPort}
      RedisHostname: ${self:custom.redisHostname}
      redisTokenKey: ${self:custom.commonVars.redisTokenKey}
      userSessionPrefix: ${self:custom.commonVars.userSessionPrefix}
      userSessionSeconds: ${self:custom.vars.userSessionSeconds}
      tokenSessionSeconds: ${self:custom.vars.tokenSessionSeconds}
      releaseVersion: ${self:custom.vars.releaseVersion}
      logLevel: ${self:custom.vars.logLevel}
      errorUsers: ${self:custom.vars.errorUsers}
      deviceSupportCountries: ${self:custom.vars.deviceSupportCountries}
      appConfigApplicationId: ${self:custom.vars.appConfigApplicationId}
      appConfigEnvironmentId: ${self:custom.vars.appConfigEnvironmentId}
      appConfigUrl: ${self:custom.vars.appConfigUrl}
    role: lambdaRole
    events:
      - http:
          path: /single
          method: get
          cors:
            origins: '*'
            headers:
              - Content-Type
              - x-api-token
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - authorizationtoken
            allowCredentials: false
          authorizer:
            name: authorizer
            arn: ${self:custom.apiauthorizerArn}
            type: request
            resultTtlInSeconds: 0
            identitySource: method.request.header.Authorization, method.request.querystring.id
    warmup:
      prewarm: true
      enabled:
        - uat
        - prd
        - dev

  SubmitServiceRequest:
    handler: src/handler.submitServiceRequest
    layers:
      - arn:aws:lambda:ap-southeast-2:080788657173:layer:AWS-AppConfig-Extension:91
    kmsKeyArn: ${self:custom.kmsArn}
    environment:
      region: ${self:custom.region}
      boomiOauthEndpoint: ${self:custom.commonVars.boomiOauthEndpoint}
      boomiServiceEndpoint: ${self:custom.commonVars.boomiServiceEndpoint}
      Environment: ${self:custom.environment}
      SFHostname: ${self:custom.commonVars.sfHostname}
      secretName: ${self:custom.secretName}
      RedisPort: ${self:custom.redisPort}
      RedisHostname: ${self:custom.redisHostname}
      redisTokenKey: ${self:custom.commonVars.redisTokenKey}
      webToCaseUrl: ${self:custom.vars.webToCaseUrl}
      sfOrgId: ${self:custom.vars.sfOrgId}
      srDescriptionJsonKey: ${self:custom.vars.srDescriptionJsonKey}
      releaseVersion: ${self:custom.vars.releaseVersion}
      logLevel: ${self:custom.vars.logLevel}
      deviceSupportCountries: ${self:custom.vars.deviceSupportCountries}
      appConfigApplicationId: ${self:custom.vars.appConfigApplicationId}
      appConfigEnvironmentId: ${self:custom.vars.appConfigEnvironmentId}
      appConfigUrl: ${self:custom.vars.appConfigUrl}
    role: lambdaRole
    events:
      - http:
          path: /service-requests
          method: POST
          request:
            schemas:
              application/json: ${file(serviceRequest-post.yml)}
          cors:
            origins: '*'
            headers:
              - Content-Type
              - x-api-token
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - authorizationtoken
            allowCredentials: false
          authorizer:
            name: authorizer
            arn: ${self:custom.apiauthorizerArn}
            type: request
            resultTtlInSeconds: 0
            identitySource: method.request.header.Authorization
    # warmup:
    #   prewarm: true
    #   enabled:
    #     - uat
    #     - prd
    #     - dev

## Resources ##
resources:
  Conditions:
    CreateAlarm:
      Fn::Equals:
        - ${self:custom.vars.createAlarms}
        - 'true'
  Resources:
    CustomDomainMapping:
      Type: AWS::ApiGateway::BasePathMapping
      DependsOn:
        - 'ApiGatewayDeployment${sls:instanceId}'
      Properties:
        RestApiId: !Ref ApiGatewayRestApi
        BasePath: ${self:custom.customDomainPath}
        DomainName:
          ${self:custom.customDomainName}
          # 'Fn::ImportValue': '${env:PROJECT_CODE}-${env:ENVIRONMENT}-api-custom-domain'
        Stage: ${self:custom.stage}
    SubmitServiceRequestLambdaFunctionErrorAlarm:
      Type: AWS::CloudWatch::Alarm
      Condition: CreateAlarm
      Properties:
        AlarmActions:
          - ${self:custom.commonVars.alarmSNS}
        AlarmDescription: 'DRX lambda fail'
        AlarmName: !Join
          - '-'
          - - 'DRX-lambda-SubmitServiceRequest-fail'
            - ${self:custom.stage}
            # - ${opt:versionNumber,"0"}
        ComparisonOperator: GreaterThanOrEqualToThreshold
        EvaluationPeriods: '1'
        Namespace: AWS/Lambda
        MetricName: Errors
        Dimensions:
          - Name: FunctionName
            Value: !Ref SubmitServiceRequestLambdaFunction
        Period: '60'
        Statistic: Sum
        Threshold: '1'
        TreatMissingData: 'notBreaching'
        OKActions:
          - ${self:custom.commonVars.alarmSNS}

    GetDevicesListLambdaFunctionErrorAlarm:
      Type: AWS::CloudWatch::Alarm
      Condition: CreateAlarm
      Properties:
        AlarmActions:
          - ${self:custom.commonVars.alarmSNS}
        AlarmDescription: 'DRX lambda fail'
        AlarmName: !Join
          - '-'
          - - 'DRX-lambda-GetDevicesList-fail'
            - ${self:custom.stage}
            # - ${opt:versionNumber,"0"}
        ComparisonOperator: GreaterThanOrEqualToThreshold
        EvaluationPeriods: '1'
        Namespace: AWS/Lambda
        MetricName: Errors
        Dimensions:
          - Name: FunctionName
            Value: !Ref GetDevicesListLambdaFunction
        Period: '60'
        Statistic: Sum
        Threshold: '1'
        TreatMissingData: 'notBreaching'
        OKActions:
          - ${self:custom.commonVars.alarmSNS}

    GatewayResponseDefault4XX:
      Type: 'AWS::ApiGateway::GatewayResponse'
      Properties:
        ResponseParameters:
          gatewayresponse.header.Access-Control-Allow-Origin: "'*'"
          gatewayresponse.header.Access-Control-Allow-Headers: "'*'"
        ResponseType: DEFAULT_4XX
        RestApiId:
          Ref: 'ApiGatewayRestApi'

    lambdaRole:
      Type: AWS::IAM::Role
      Properties:
        Path: /drx/all/
        PermissionsBoundary: !Sub 'arn:aws:iam::${AWS::AccountId}:policy/core/PermissionsBoundary'
        ManagedPolicyArns: [
            'arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole',
            'arn:aws:iam::aws:policy/AWSXRayDaemonWriteAccess'
          ] ## Allows VPC Execution rights for Lambda
        AssumeRolePolicyDocument:
          Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Principal:
                Service:
                  - lambda.amazonaws.com
              Action: sts:AssumeRole
        Policies:
          - PolicyName: LambdaDRX
            PolicyDocument:
              Version: '2012-10-17'
              Statement:
                - Effect: Allow # Default Lambda Persmissions for Cloudwatch Log Access
                  Resource: arn:aws:logs:${self:custom.region}:*:log-group:/aws/lambda/*:*:*
                  Action:
                    - logs:CreateLogGroup
                    - logs:CreateLogStream
                    - logs:PutLogEvents
                - Effect: Allow
                  Resource:
                    - arn:aws:ssm:${self:custom.region}:*:parameter/*
                  Action:
                    - ssm:GetParameter
                - Effect: Allow
                  Resource:
                    - arn:aws:secretsmanager:${self:custom.region}:*:*
                  Action:
                    - secretsmanager:GetSecretValue
                - Effect: 'Allow'
                  Resource:
                    - !Sub
                      - 'arn:aws:appconfig:${AWS::Region}:${AWS::AccountId}:application/${ApplicationId}/environment/${EnvironmentId}/configuration/*'
                      - ApplicationId: ${self:custom.vars.appConfigApplicationId}
                        EnvironmentId: ${self:custom.vars.appConfigEnvironmentId}
                  Action:
                    - appconfig:GetLatestConfiguration
                    - appconfig:StartConfigurationSession
    # warmupRole:
    #   Type: AWS::IAM::Role
    #   Properties:
    #     Path: /drx/all/
    #     ManagedPolicyArns: [
    #         'arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole',
    #       ] ## Allows VPC Execution rights for Lambda
    #     AssumeRolePolicyDocument:
    #       Version: '2012-10-17'
    #       Statement:
    #         - Effect: Allow
    #           Principal:
    #             Service:
    #               - lambda.amazonaws.com
    #           Action: sts:AssumeRole
    #     Policies:
    #       - PolicyName: LambdaWarmupDRX
    #         PolicyDocument:
    #           Version: '2012-10-17'
    #           Statement:
    #             - Effect: Allow # WarmUp lamda to send logs to CloudWatch
    #               Action:
    #                 - logs:CreateLogGroup
    #                 - logs:CreateLogStream
    #                 - logs:PutLogEvents
    #               Resource:
    #                 - 'Fn::Join':
    #                     - ':'
    #                     - - 'arn:aws:logs'
    #                       - Ref: 'AWS::Region'
    #                       - Ref: 'AWS::AccountId'
    #                       - 'log-group:/aws/lambda/*:*:*'
    #             - Effect: Allow # WarmUp lamda to manage ENIS (only needed if deploying to VPC, https://docs.aws.amazon.com/lambda/latest/dg/vpc.html)
    #               Action:
    #                 - ec2:CreateNetworkInterface
    #                 - ec2:DescribeNetworkInterfaces
    #                 - ec2:DetachNetworkInterface
    #                 - ec2:DeleteNetworkInterface
    #               Resource: '*'
    #             - Effect: 'Allow' # WarmUp lamda to invoke the functions to be warmed
    #               Action:
    #                 - 'lambda:InvokeFunction'
    #               Resource:
    #                 - Fn::Join:
    #                     - ':'
    #                     - - arn:aws:lambda
    #                       - Ref: AWS::Region
    #                       - Ref: AWS::AccountId
    #                       - function:${self:service}-${opt:stage, self:provider.stage}-*
  Outputs:
    RestApiId:
      Description: 'rest api id'
      Value:
        Ref: ApiGatewayRestApi
