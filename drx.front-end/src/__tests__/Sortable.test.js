
describe("SortableTable", () => {

    test('render', () => {
        //expected result : render 

    });

    test('table contains columns as defined in config', () => {
    
        //expected result : all columns should be present

    });

});


describe("SortableTable / Column", () => {

    
    test('display currency as localized format', () => {

        //expected result : currency display should match format which in (test)

    });

    test('display dateTime as localized format', () => {

        //expected result : datetime display should match format (test with different country formats)

    });

    
});
